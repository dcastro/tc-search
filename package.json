{
  "private": true,
  "scripts": {
    "build": "pulp browserify --optimise --to dist/app.js",
    "watch": "pulp --watch browserify --to dist/app.js"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "pulp": "^10.0.0",
    "purescript": "^0.10.5"
  }
}
